{{- if not .Values.dependenciesOnly -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-cnp-dashboard
  labels:
    grafana_dashboard: "1"
    {{- include "cnp-playground.labels" . | nindent 4 }}
data:
  cnp.json: |-
{{ .Files.Get "dashboard.json" | indent 6 }}
{{ end -}}
