{{- if and (not .Values.dependenciesOnly) (not .Release.IsInstall) -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-cnp-dashboard
  labels:
    grafana_dashboard: "1"
data:
  cnp.json: |-
{{ .Files.Get "dashboard.json" | indent 6 }}
{{ end -}}
