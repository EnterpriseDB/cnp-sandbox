{{- if .Values.dependenciesOnly -}}
Now run:

helm upgrade --install --set dependenciesOnly=false {{ .Release.Name }} <PATH_TO_CHART>
{{- else -}}
Now you can access to Grafana with the following credentials:

username: "admin"
password: "{{ index .Values "kube-prometheus-stack" "grafana" "adminPassword" }}"

by executing the following command:

kubectl port-forward svc/{{.Release.Name}}-grafana 3000:80

and going to: http://localhost:3000
{{- end -}}
